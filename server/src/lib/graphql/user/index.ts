import {query} from './queries'
import {typeDef} from './typeDef'
import {mutation} from './mutations'
import {resolvers} from './resolvers'
export const User = {query, typeDef, mutation, resolvers}

